[package]
name = "minigrep"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1.0.80"
dotenv = "0.15.0"
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
